spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:focuson}
spring.datasource.username=${POSTGRES_USER:focuson}
spring.datasource.password=${POSTGRES_PASSWORD}
