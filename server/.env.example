# Local development example only. Replace this value in .env and never commit .env.
POSTGRES_DB=focuson
POSTGRES_USER=focuson
POSTGRES_PASSWORD=replace-with-a-local-secret
POSTGRES_HOST_PORT=5432
BACKEND_HOST_PORT=8080
DB_MAX_POOL_SIZE=10
